# 子计划 1：持久化数据层（SQLite + sqlite-vec）实施计划

> **致执行者：** 用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans` 逐任务执行。步骤用 `- [ ]` 复选框跟踪。先读 `2026-06-28-phase1-计划索引.md` 与 `docs/specs/2026-06-28-金融市场brief-设计文档.md` 第 5 节。

**目标：** 为系统建立持久化关系层——SQLite schema（设计文档四层数据模型 + taxonomy/entity/watchlist）、连接封装、Pydantic 行模型、Repository(DAO) 层；其中 `raw_item` 支持按全局 `dedup_key` 幂等 upsert，为跨天去重打底。

**架构：** 单一 SQLite 文件（`data/marketnews.db`），开启外键约束；通过 sqlite-vec 扩展提供向量表用于后续相似度检索。Repository 层封装所有 SQL，上层只见类型化方法，不写裸 SQL。所有行数据进出用 Pydantic 模型校验。

**技术栈：** Python 3.11+、内置 `sqlite3`、`sqlite-vec`、`pydantic>=2`、`pytest`。

## Global Constraints
- Python 3.11+，沿用 Horizon 的 `uv` + `pyproject.toml`。
- 数据库文件路径默认 `data/marketnews.db`，可由 `Database(path)` 覆盖（测试用 `:memory:` 或临时文件）。
- 所有时间戳以 UTC ISO8601 字符串存储（`TEXT`）。
- JSON 字段以 `TEXT` 存储，存取时 `json.dumps/loads`。
- 外键约束必须开启（`PRAGMA foreign_keys=ON`）。
- 新代码放在 `src/db/` 下，不破坏 Horizon 既有 `src/storage/`（配置仍由其管理）。
- TDD：先写失败测试，再实现，频繁提交。

---

## 文件结构

- 创建 `src/db/__init__.py` — 导出 `Database` 及各 Repository。
- 创建 `src/db/schema.sql` — 全部建表 DDL（含索引、向量表）。
- 创建 `src/db/database.py` — `Database` 连接封装：加载 sqlite-vec、应用 schema、提供连接/事务。
- 创建 `src/db/rows.py` — Pydantic 行模型（`RawItemRow`、`StoryRow`、`ScoreRow`、`BriefRow`、`BriefItemRow`、`TaxonomyRow`、`EntityRow` 等）。
- 创建 `src/db/repositories/__init__.py`
- 创建 `src/db/repositories/raw_item_repo.py` — `RawItemRepo`
- 创建 `src/db/repositories/story_repo.py` — `StoryRepo`、`StoryMemberRepo`
- 创建 `src/db/repositories/analysis_repo.py` — `ScoreRepo`、`EnrichmentRepo`、`StoryTagRepo`
- 创建 `src/db/repositories/brief_repo.py` — `BriefRepo`、`BriefItemRepo`
- 创建 `src/db/repositories/taxonomy_repo.py` — `TaxonomyRepo`
- 创建 `src/db/repositories/entity_repo.py` — `EntityRepo`、`StoryEntityRepo`、`WatchlistRepo`
- 创建 `src/db/seed_taxonomy.py` — 预置受控词表初始值
- 创建 `tests/db/` 下对应测试文件。

---

## Task 1: SQLite Schema（建表 DDL）

**Files:**
- Create: `src/db/schema.sql`
- Test: `tests/db/test_schema.py`

**Interfaces:**
- Produces: 一个可被 `executescript` 应用的 DDL 文件；建立全部表与索引。后续所有 Repo 依赖这些表名与列名。

- [ ] **Step 1: 写 schema.sql（完整 DDL）**

```sql
-- ============ 摄取层 ============
CREATE TABLE IF NOT EXISTS source (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  name         TEXT NOT NULL,
  type         TEXT NOT NULL,              -- rss / edgar / hkex / api
  url          TEXT,
  trust_tier   INTEGER NOT NULL DEFAULT 2, -- 1 最权威 .. 3 一般
  language     TEXT,
  category     TEXT,
  enabled      INTEGER NOT NULL DEFAULT 1,
  fetch_config TEXT,                        -- JSON
  created_at   TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS raw_item (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  source_id     INTEGER REFERENCES source(id),
  external_id   TEXT,                       -- 源自带 guid
  url           TEXT,
  canonical_url TEXT,
  title         TEXT NOT NULL,
  summary       TEXT,
  author        TEXT,
  published_at  TEXT,
  fetched_at    TEXT NOT NULL,
  dedup_key     TEXT NOT NULL UNIQUE,       -- 规范URL+内容哈希，全局持久化
  content_hash  TEXT,
  language      TEXT,
  raw_payload   TEXT                        -- JSON 原始全量
);
CREATE INDEX IF NOT EXISTS idx_raw_item_published ON raw_item(published_at);
CREATE INDEX IF NOT EXISTS idx_raw_item_source ON raw_item(source_id);

-- ============ 加工层 ============
CREATE TABLE IF NOT EXISTS story (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  canonical_title   TEXT NOT NULL,
  dedup_cluster_key TEXT,
  first_seen_at     TEXT NOT NULL,
  last_seen_at      TEXT NOT NULL,
  last_update_at    TEXT,
  status            TEXT NOT NULL DEFAULT 'new'  -- new/ongoing/updated/dormant
);
CREATE INDEX IF NOT EXISTS idx_story_status ON story(status);
CREATE INDEX IF NOT EXISTS idx_story_last_seen ON story(last_seen_at);

CREATE TABLE IF NOT EXISTS story_member (
  story_id    INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  raw_item_id INTEGER NOT NULL REFERENCES raw_item(id) ON DELETE CASCADE,
  is_primary  INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (story_id, raw_item_id)
);

CREATE TABLE IF NOT EXISTS score (
  id                   INTEGER PRIMARY KEY AUTOINCREMENT,
  story_id             INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  model                TEXT,
  score                REAL NOT NULL,        -- 0-10
  importance_rationale TEXT,
  scored_at            TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_score_story ON score(story_id);

CREATE TABLE IF NOT EXISTS enrichment (
  id                   INTEGER PRIMARY KEY AUTOINCREMENT,
  story_id             INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  context_text         TEXT,
  corroborating_sources TEXT,                -- JSON
  confidence           TEXT,                 -- confirmed / unconfirmed
  created_at           TEXT NOT NULL
);

-- ============ 分类 / 实体 ============
CREATE TABLE IF NOT EXISTS taxonomy (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  dimension  TEXT NOT NULL,   -- market_type/industry_group/product_group/region/asset_class
  code       TEXT NOT NULL,
  label      TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  UNIQUE (dimension, code)
);

CREATE TABLE IF NOT EXISTS story_tag (
  story_id    INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  taxonomy_id INTEGER NOT NULL REFERENCES taxonomy(id) ON DELETE CASCADE,
  PRIMARY KEY (story_id, taxonomy_id)
);

CREATE TABLE IF NOT EXISTS entity (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  type        TEXT NOT NULL,   -- company/ticker/sector/person
  name        TEXT NOT NULL,
  ticker      TEXT,
  identifiers TEXT,            -- JSON: ISIN/CIK 等
  created_at  TEXT NOT NULL,
  UNIQUE (type, name)
);

CREATE TABLE IF NOT EXISTS story_entity (
  story_id  INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  entity_id INTEGER NOT NULL REFERENCES entity(id) ON DELETE CASCADE,
  role      TEXT,              -- primary / related
  PRIMARY KEY (story_id, entity_id)
);

-- ============ 输出层 ============
CREATE TABLE IF NOT EXISTS brief (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  period_type      TEXT NOT NULL,   -- daily / 12h
  period_date      TEXT NOT NULL,
  language         TEXT NOT NULL DEFAULT 'zh',
  model            TEXT,
  generated_at     TEXT NOT NULL,
  status           TEXT NOT NULL DEFAULT 'draft',
  market_view_text TEXT,            -- 整期综合市场观点
  UNIQUE (period_type, period_date, language)
);

CREATE TABLE IF NOT EXISTS brief_item (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  brief_id   INTEGER NOT NULL REFERENCES brief(id) ON DELETE CASCADE,
  story_id   INTEGER NOT NULL REFERENCES story(id) ON DELETE CASCADE,
  rank       INTEGER NOT NULL,
  headline   TEXT,
  summary    TEXT,
  view_text  TEXT,                  -- 单条中性影响陈述
  created_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_brief_item_brief ON brief_item(brief_id);

-- ============ 产品层（Phase 2 预留，本阶段不写入） ============
CREATE TABLE IF NOT EXISTS app_user (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  email      TEXT UNIQUE,
  name       TEXT,
  plan       TEXT,
  created_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS subscription (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id     INTEGER REFERENCES app_user(id) ON DELETE CASCADE,
  config      TEXT,   -- JSON: 订阅的 source/topic/阈值/渠道/语言
  created_at  TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS delivery (
  id        INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id   INTEGER REFERENCES app_user(id) ON DELETE CASCADE,
  brief_id  INTEGER REFERENCES brief(id) ON DELETE CASCADE,
  channel   TEXT,
  sent_at   TEXT,
  status    TEXT
);
CREATE TABLE IF NOT EXISTS event (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id       INTEGER REFERENCES app_user(id) ON DELETE CASCADE,
  brief_item_id INTEGER REFERENCES brief_item(id) ON DELETE CASCADE,
  type          TEXT,   -- open / click / dwell
  ts            TEXT
);
CREATE TABLE IF NOT EXISTS feedback (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id       INTEGER REFERENCES app_user(id) ON DELETE CASCADE,
  brief_item_id INTEGER REFERENCES brief_item(id) ON DELETE CASCADE,
  rating        INTEGER,
  comment       TEXT,
  ts            TEXT
);

CREATE TABLE IF NOT EXISTS watchlist_item (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id    INTEGER,            -- Phase1 用固定默认用户 id=1（不强制外键）
  entity_id  INTEGER NOT NULL REFERENCES entity(id) ON DELETE CASCADE,
  note       TEXT,
  added_at   TEXT NOT NULL,
  UNIQUE (user_id, entity_id)
);
```

- [ ] **Step 2: 写测试 `tests/db/test_schema.py`**

```python
import sqlite3
from pathlib import Path

SCHEMA = Path("src/db/schema.sql").read_text(encoding="utf-8")

def test_schema_creates_core_tables():
    conn = sqlite3.connect(":memory:")
    conn.executescript(SCHEMA)
    names = {r[0] for r in conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table'"
    ).fetchall()}
    expected = {
        "source", "raw_item", "story", "story_member", "score", "enrichment",
        "taxonomy", "story_tag", "entity", "story_entity", "brief", "brief_item",
        "watchlist_item", "app_user", "subscription", "delivery", "event", "feedback",
    }
    assert expected <= names

def test_raw_item_dedup_key_unique():
    conn = sqlite3.connect(":memory:")
    conn.executescript(SCHEMA)
    conn.execute("PRAGMA foreign_keys=ON")
    conn.execute(
        "INSERT INTO raw_item (title, fetched_at, dedup_key) VALUES (?,?,?)",
        ("a", "2026-06-28T00:00:00Z", "key1"),
    )
    import pytest
    with pytest.raises(sqlite3.IntegrityError):
        conn.execute(
            "INSERT INTO raw_item (title, fetched_at, dedup_key) VALUES (?,?,?)",
            ("b", "2026-06-28T00:00:00Z", "key1"),
        )
```

- [ ] **Step 3: 跑测试确认通过**

Run: `pytest tests/db/test_schema.py -v`
Expected: 2 passed

- [ ] **Step 4: 提交**

```bash
git add src/db/schema.sql tests/db/test_schema.py
git commit -m "feat(db): add SQLite schema for persistence layer"
```

---

## Task 2: Database 连接封装（含 sqlite-vec + 向量表）

**Files:**
- Create: `src/db/database.py`
- Test: `tests/db/test_database.py`

**Interfaces:**
- Consumes: `src/db/schema.sql`
- Produces:
  - `class Database(path: str = "data/marketnews.db")`
  - `Database.connect() -> sqlite3.Connection`（开启 `foreign_keys=ON`、`row_factory=sqlite3.Row`、加载 sqlite-vec）
  - `Database.init_schema() -> None`（应用 schema.sql + 建向量表 `vec_raw_item`）
  - `Database.transaction()` 上下文管理器（commit/rollback）

- [ ] **Step 1: 写测试 `tests/db/test_database.py`**

```python
from src.db.database import Database

def test_init_schema_and_foreign_keys(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    conn = db.connect()
    assert conn.execute("PRAGMA foreign_keys").fetchone()[0] == 1
    tables = {r["name"] for r in conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table'")}
    assert "raw_item" in tables

def test_vec_table_knn(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    conn = db.connect()
    conn.execute("INSERT INTO vec_raw_item(rowid, embedding) VALUES (?, ?)",
                 (1, _vec([1.0, 0.0, 0.0])))
    conn.execute("INSERT INTO vec_raw_item(rowid, embedding) VALUES (?, ?)",
                 (2, _vec([0.0, 1.0, 0.0])))
    conn.commit()
    rows = conn.execute(
        "SELECT rowid FROM vec_raw_item "
        "WHERE embedding MATCH ? ORDER BY distance LIMIT 1",
        (_vec([0.9, 0.1, 0.0]),)).fetchall()
    assert rows[0]["rowid"] == 1

def _vec(values):
    import struct
    return struct.pack(f"{len(values)}f", *values)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/db/test_database.py -v`
Expected: FAIL（ModuleNotFoundError: src.db.database）

- [ ] **Step 3: 实现 `src/db/database.py`**

```python
"""SQLite 连接封装，加载 sqlite-vec 并应用 schema。"""
from __future__ import annotations
import sqlite3
from contextlib import contextmanager
from pathlib import Path
import sqlite_vec

_SCHEMA_PATH = Path(__file__).parent / "schema.sql"
# 与 embedding 模型维度一致（paraphrase-multilingual-MiniLM-L12-v2 = 384）
EMBEDDING_DIM = 384


class Database:
    def __init__(self, path: str = "data/marketnews.db"):
        self.path = path
        if path != ":memory:":
            Path(path).parent.mkdir(parents=True, exist_ok=True)

    def connect(self) -> sqlite3.Connection:
        conn = sqlite3.connect(self.path)
        conn.row_factory = sqlite3.Row
        conn.enable_load_extension(True)
        sqlite_vec.load(conn)
        conn.enable_load_extension(False)
        conn.execute("PRAGMA foreign_keys=ON")
        return conn

    def init_schema(self) -> None:
        conn = self.connect()
        try:
            conn.executescript(_SCHEMA_PATH.read_text(encoding="utf-8"))
            conn.execute(
                f"CREATE VIRTUAL TABLE IF NOT EXISTS vec_raw_item "
                f"USING vec0(embedding float[{EMBEDDING_DIM}])"
            )
            conn.commit()
        finally:
            conn.close()

    @contextmanager
    def transaction(self):
        conn = self.connect()
        try:
            yield conn
            conn.commit()
        except Exception:
            conn.rollback()
            raise
        finally:
            conn.close()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/db/test_database.py -v`
Expected: 2 passed（若缺包先 `uv add sqlite-vec`）

- [ ] **Step 5: 提交**

```bash
git add src/db/database.py tests/db/test_database.py pyproject.toml
git commit -m "feat(db): add Database wrapper with sqlite-vec vector table"
```

---

## Task 3: Pydantic 行模型

**Files:**
- Create: `src/db/rows.py`
- Test: `tests/db/test_rows.py`

**Interfaces:**
- Produces: `RawItemRow`、`StoryRow`、`ScoreRow`、`TaxonomyRow`、`EntityRow`、`BriefRow`、`BriefItemRow`（均为 `pydantic.BaseModel`，字段与对应表列对齐，id 可空表示未入库）。后续 Repo 的入参/返回值用这些类型。

- [ ] **Step 1: 写测试 `tests/db/test_rows.py`**

```python
from src.db.rows import RawItemRow

def test_raw_item_row_roundtrip():
    row = RawItemRow(
        title="Acme acquires Beta", fetched_at="2026-06-28T00:00:00Z",
        dedup_key="k1", url="https://x.com/a",
    )
    assert row.id is None
    assert row.dedup_key == "k1"
    d = row.model_dump()
    assert d["title"] == "Acme acquires Beta"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/db/test_rows.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 `src/db/rows.py`**

```python
"""Pydantic 行模型，与 schema.sql 各表列对齐。id 为空表示尚未入库。"""
from __future__ import annotations
from typing import Optional, Any
from pydantic import BaseModel


class RawItemRow(BaseModel):
    id: Optional[int] = None
    source_id: Optional[int] = None
    external_id: Optional[str] = None
    url: Optional[str] = None
    canonical_url: Optional[str] = None
    title: str
    summary: Optional[str] = None
    author: Optional[str] = None
    published_at: Optional[str] = None
    fetched_at: str
    dedup_key: str
    content_hash: Optional[str] = None
    language: Optional[str] = None
    raw_payload: Optional[str] = None


class StoryRow(BaseModel):
    id: Optional[int] = None
    canonical_title: str
    dedup_cluster_key: Optional[str] = None
    first_seen_at: str
    last_seen_at: str
    last_update_at: Optional[str] = None
    status: str = "new"


class ScoreRow(BaseModel):
    id: Optional[int] = None
    story_id: int
    model: Optional[str] = None
    score: float
    importance_rationale: Optional[str] = None
    scored_at: str


class TaxonomyRow(BaseModel):
    id: Optional[int] = None
    dimension: str
    code: str
    label: str
    sort_order: int = 0


class EntityRow(BaseModel):
    id: Optional[int] = None
    type: str
    name: str
    ticker: Optional[str] = None
    identifiers: Optional[str] = None
    created_at: str


class BriefRow(BaseModel):
    id: Optional[int] = None
    period_type: str
    period_date: str
    language: str = "zh"
    model: Optional[str] = None
    generated_at: str
    status: str = "draft"
    market_view_text: Optional[str] = None


class BriefItemRow(BaseModel):
    id: Optional[int] = None
    brief_id: int
    story_id: int
    rank: int
    headline: Optional[str] = None
    summary: Optional[str] = None
    view_text: Optional[str] = None
    created_at: str
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/db/test_rows.py -v`
Expected: 1 passed

- [ ] **Step 5: 提交**

```bash
git add src/db/rows.py tests/db/test_rows.py
git commit -m "feat(db): add Pydantic row models"
```

---

## Task 4: RawItemRepo（★幂等 upsert + 全局去重）

**Files:**
- Create: `src/db/repositories/__init__.py`（空）
- Create: `src/db/repositories/raw_item_repo.py`
- Test: `tests/db/test_raw_item_repo.py`

**Interfaces:**
- Consumes: `Database`、`RawItemRow`
- Produces:
  - `RawItemRepo(db: Database)`
  - `upsert(item: RawItemRow) -> tuple[int, bool]` — 按 `dedup_key` 查重：已存在返回 `(existing_id, False)` 且不重复插入（**零额外处理，去重在模型调用前**）；不存在则插入返回 `(new_id, True)`。
  - `get(id: int) -> Optional[RawItemRow]`
  - `get_by_dedup_key(key: str) -> Optional[RawItemRow]`

- [ ] **Step 1: 写测试 `tests/db/test_raw_item_repo.py`**

```python
from src.db.database import Database
from src.db.rows import RawItemRow
from src.db.repositories.raw_item_repo import RawItemRepo

def _repo(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    return RawItemRepo(db)

def _item(key):
    return RawItemRow(title="t", fetched_at="2026-06-28T00:00:00Z", dedup_key=key)

def test_upsert_inserts_new(tmp_path):
    repo = _repo(tmp_path)
    new_id, is_new = repo.upsert(_item("k1"))
    assert is_new is True
    assert new_id > 0

def test_upsert_is_idempotent_on_dedup_key(tmp_path):
    repo = _repo(tmp_path)
    id1, new1 = repo.upsert(_item("dup"))
    id2, new2 = repo.upsert(_item("dup"))
    assert new1 is True and new2 is False
    assert id1 == id2

def test_get_by_dedup_key(tmp_path):
    repo = _repo(tmp_path)
    repo.upsert(_item("k2"))
    got = repo.get_by_dedup_key("k2")
    assert got is not None and got.dedup_key == "k2"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/db/test_raw_item_repo.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 `src/db/repositories/raw_item_repo.py`**

```python
"""raw_item 仓储：按 dedup_key 幂等 upsert，实现全局持久化去重。"""
from __future__ import annotations
from typing import Optional
from ..database import Database
from ..rows import RawItemRow

_COLS = [
    "source_id", "external_id", "url", "canonical_url", "title", "summary",
    "author", "published_at", "fetched_at", "dedup_key", "content_hash",
    "language", "raw_payload",
]


class RawItemRepo:
    def __init__(self, db: Database):
        self.db = db

    def upsert(self, item: RawItemRow) -> tuple[int, bool]:
        with self.db.transaction() as conn:
            existing = conn.execute(
                "SELECT id FROM raw_item WHERE dedup_key = ?", (item.dedup_key,)
            ).fetchone()
            if existing is not None:
                return existing["id"], False
            placeholders = ", ".join("?" for _ in _COLS)
            values = [getattr(item, c) for c in _COLS]
            cur = conn.execute(
                f"INSERT INTO raw_item ({', '.join(_COLS)}) VALUES ({placeholders})",
                values,
            )
            return cur.lastrowid, True

    def get(self, id: int) -> Optional[RawItemRow]:
        conn = self.db.connect()
        try:
            row = conn.execute("SELECT * FROM raw_item WHERE id = ?", (id,)).fetchone()
            return RawItemRow(**dict(row)) if row else None
        finally:
            conn.close()

    def get_by_dedup_key(self, key: str) -> Optional[RawItemRow]:
        conn = self.db.connect()
        try:
            row = conn.execute(
                "SELECT * FROM raw_item WHERE dedup_key = ?", (key,)
            ).fetchone()
            return RawItemRow(**dict(row)) if row else None
        finally:
            conn.close()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/db/test_raw_item_repo.py -v`
Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add src/db/repositories/__init__.py src/db/repositories/raw_item_repo.py tests/db/test_raw_item_repo.py
git commit -m "feat(db): add RawItemRepo with idempotent dedup upsert"
```

---

## Task 5: TaxonomyRepo + 受控词表种子

**Files:**
- Create: `src/db/repositories/taxonomy_repo.py`
- Create: `src/db/seed_taxonomy.py`
- Test: `tests/db/test_taxonomy.py`

**Interfaces:**
- Consumes: `Database`、`TaxonomyRow`
- Produces:
  - `TaxonomyRepo(db)`：`upsert(row: TaxonomyRow) -> int`（按 `(dimension, code)` 幂等）、`get_id(dimension, code) -> Optional[int]`、`list_by_dimension(dimension) -> list[TaxonomyRow]`
  - `seed_taxonomy(db: Database) -> int`：写入预置词表，返回写入条数。

- [ ] **Step 1: 写测试 `tests/db/test_taxonomy.py`**

```python
from src.db.database import Database
from src.db.repositories.taxonomy_repo import TaxonomyRepo
from src.db.seed_taxonomy import seed_taxonomy

def _db(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    return db

def test_seed_is_idempotent(tmp_path):
    db = _db(tmp_path)
    n1 = seed_taxonomy(db)
    n2 = seed_taxonomy(db)
    repo = TaxonomyRepo(db)
    market = repo.list_by_dimension("market_type")
    assert n1 > 0
    assert {m.code for m in market} == {"primary", "secondary"}
    # 再次 seed 不应重复增加
    assert len(repo.list_by_dimension("market_type")) == 2

def test_get_id(tmp_path):
    db = _db(tmp_path)
    seed_taxonomy(db)
    repo = TaxonomyRepo(db)
    assert repo.get_id("industry_group", "TMT") is not None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/db/test_taxonomy.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 `src/db/repositories/taxonomy_repo.py`**

```python
"""taxonomy 仓储。"""
from __future__ import annotations
from typing import Optional
from ..database import Database
from ..rows import TaxonomyRow


class TaxonomyRepo:
    def __init__(self, db: Database):
        self.db = db

    def upsert(self, row: TaxonomyRow) -> int:
        with self.db.transaction() as conn:
            existing = conn.execute(
                "SELECT id FROM taxonomy WHERE dimension=? AND code=?",
                (row.dimension, row.code),
            ).fetchone()
            if existing:
                return existing["id"]
            cur = conn.execute(
                "INSERT INTO taxonomy (dimension, code, label, sort_order) "
                "VALUES (?,?,?,?)",
                (row.dimension, row.code, row.label, row.sort_order),
            )
            return cur.lastrowid

    def get_id(self, dimension: str, code: str) -> Optional[int]:
        conn = self.db.connect()
        try:
            r = conn.execute(
                "SELECT id FROM taxonomy WHERE dimension=? AND code=?",
                (dimension, code),
            ).fetchone()
            return r["id"] if r else None
        finally:
            conn.close()

    def list_by_dimension(self, dimension: str) -> list[TaxonomyRow]:
        conn = self.db.connect()
        try:
            rows = conn.execute(
                "SELECT * FROM taxonomy WHERE dimension=? ORDER BY sort_order, id",
                (dimension,),
            ).fetchall()
            return [TaxonomyRow(**dict(r)) for r in rows]
        finally:
            conn.close()
```

- [ ] **Step 4: 实现 `src/db/seed_taxonomy.py`**

```python
"""预置受控词表（设计文档第 5 节五轴）。"""
from __future__ import annotations
from .database import Database
from .repositories.taxonomy_repo import TaxonomyRepo
from .rows import TaxonomyRow

_SEED = {
    "market_type": [("primary", "一级市场"), ("secondary", "二级市场")],
    "industry_group": [
        ("TMT", "TMT"), ("Consumer", "Consumer & Retail"),
        ("Healthcare", "Healthcare"), ("Energy", "Energy & Natural Resources"),
        ("Industrials", "Industrials"), ("FIG", "Financial Institutions"),
        ("RealEstate", "Real Estate"),
    ],
    "product_group": [
        ("MA", "M&A / Advisory"), ("ECM", "ECM"), ("DCM", "DCM"),
        ("LevFin", "Leveraged Finance"), ("Restructuring", "Restructuring"),
        ("VC", "VC融资"), ("PEBuyout", "PE Buyout"),
        ("FundClosing", "基金募集"), ("PreIPO", "Pre-IPO"),
        ("PrivatePlacement", "私募配售"),
    ],
    "region": [
        ("NA", "北美"), ("EU", "欧洲"), ("GreaterChina", "大中华(含香港)"),
        ("JPKR", "日韩"), ("SEA_India", "东南亚·印度"),
        ("EM", "新兴市场"), ("Global", "全球·跨区域"),
    ],
    "asset_class": [
        ("Equity", "股票"), ("Credit", "债券"), ("PE", "私募股权"),
        ("FX", "外汇"), ("Commodity", "大宗商品"),
    ],
}


def seed_taxonomy(db: Database) -> int:
    repo = TaxonomyRepo(db)
    n = 0
    for dimension, items in _SEED.items():
        for order, (code, label) in enumerate(items):
            repo.upsert(TaxonomyRow(
                dimension=dimension, code=code, label=label, sort_order=order))
            n += 1
    return n
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pytest tests/db/test_taxonomy.py -v`
Expected: 2 passed

- [ ] **Step 6: 提交**

```bash
git add src/db/repositories/taxonomy_repo.py src/db/seed_taxonomy.py tests/db/test_taxonomy.py
git commit -m "feat(db): add TaxonomyRepo and controlled-vocabulary seed"
```

---

## Task 6: 其余仓储（Story / Score / Enrichment / StoryTag / Entity / Brief / Watchlist）

按 Task 4/5 的同一模式实现以下仓储。每个方法都遵循「`db.transaction()` 写、`db.connect()` 读、行用 Pydantic 模型」的范式。每个仓储先写失败测试再实现，单独提交。

**Files:**
- Create: `src/db/repositories/story_repo.py` → `StoryRepo`、`StoryMemberRepo`
- Create: `src/db/repositories/analysis_repo.py` → `ScoreRepo`、`EnrichmentRepo`、`StoryTagRepo`
- Create: `src/db/repositories/entity_repo.py` → `EntityRepo`、`StoryEntityRepo`、`WatchlistRepo`
- Create: `src/db/repositories/brief_repo.py` → `BriefRepo`、`BriefItemRepo`
- Test: 对应 `tests/db/test_*.py`

**Interfaces（后续子计划依赖的方法签名，必须实现）：**

```python
# story_repo.py
class StoryRepo:
    def create(self, row: StoryRow) -> int: ...
    def get(self, id: int) -> Optional[StoryRow]: ...
    def update_status(self, id: int, status: str, last_update_at: str) -> None: ...
    def recent(self, since_iso: str) -> list[StoryRow]: ...   # 跨天聚类用（子计划3）

class StoryMemberRepo:
    def add(self, story_id: int, raw_item_id: int, is_primary: bool = False) -> None: ...
    def raw_items_for_story(self, story_id: int) -> list[int]: ...

# analysis_repo.py
class ScoreRepo:
    def add(self, row: ScoreRow) -> int: ...
    def latest_for_story(self, story_id: int) -> Optional[ScoreRow]: ...

class EnrichmentRepo:
    def add(self, story_id: int, context_text: str,
            corroborating_sources: str, confidence: str, created_at: str) -> int: ...

class StoryTagRepo:
    def add(self, story_id: int, taxonomy_id: int) -> None: ...   # 幂等(主键去重)
    def taxonomy_ids_for_story(self, story_id: int) -> list[int]: ...

# entity_repo.py
class EntityRepo:
    def upsert(self, row: EntityRow) -> int: ...   # 按 (type, name) 幂等
    def get(self, id: int) -> Optional[EntityRow]: ...

class StoryEntityRepo:
    def add(self, story_id: int, entity_id: int, role: str) -> None: ...
    def stories_for_entity(self, entity_id: int) -> list[int]: ...   # 单公司回顾(子计划6)

class WatchlistRepo:
    def add(self, entity_id: int, note: str, added_at: str, user_id: int = 1) -> int: ...
    def list(self, user_id: int = 1) -> list[int]: ...   # 返回 entity_id 列表

# brief_repo.py
class BriefRepo:
    def create(self, row: BriefRow) -> int: ...
    def get(self, id: int) -> Optional[BriefRow]: ...
    def set_market_view(self, id: int, market_view_text: str) -> None: ...

class BriefItemRepo:
    def add(self, row: BriefItemRow) -> int: ...
    def list_for_brief(self, brief_id: int) -> list[BriefItemRow]: ...
```

- [ ] **Step 1:** 为 `StoryRepo.create/get/update_status/recent` 写失败测试 → 实现 → 跑通 → 提交。
- [ ] **Step 2:** 为 `StoryMemberRepo` 写失败测试 → 实现 → 跑通 → 提交。
- [ ] **Step 3:** 为 `ScoreRepo` / `EnrichmentRepo` / `StoryTagRepo` 写失败测试 → 实现 → 跑通 → 提交。
- [ ] **Step 4:** 为 `EntityRepo`（含 `(type,name)` 幂等）/ `StoryEntityRepo` / `WatchlistRepo` 写失败测试 → 实现 → 跑通 → 提交。
- [ ] **Step 5:** 为 `BriefRepo` / `BriefItemRepo` 写失败测试 → 实现 → 跑通 → 提交。

> 实现样例（StoryRepo.create，供其余仓储照搬范式）：
> ```python
> def create(self, row: StoryRow) -> int:
>     with self.db.transaction() as conn:
>         cur = conn.execute(
>             "INSERT INTO story (canonical_title, dedup_cluster_key, "
>             "first_seen_at, last_seen_at, last_update_at, status) "
>             "VALUES (?,?,?,?,?,?)",
>             (row.canonical_title, row.dedup_cluster_key, row.first_seen_at,
>              row.last_seen_at, row.last_update_at, row.status),
>         )
>         return cur.lastrowid
> ```

---

## Task 7: 导出与初始化入口

**Files:**
- Modify: `src/db/__init__.py`
- Create: `tests/db/test_package_api.py`

**Interfaces:**
- Produces: `from src.db import Database, init_db`；`init_db(path) -> Database`（建 schema + seed 词表，幂等）。

- [ ] **Step 1: 写测试**

```python
from src.db import Database, init_db

def test_init_db(tmp_path):
    db = init_db(str(tmp_path / "t.db"))
    assert isinstance(db, Database)
    conn = db.connect()
    n = conn.execute("SELECT COUNT(*) FROM taxonomy").fetchone()[0]
    assert n > 0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/db/test_package_api.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `src/db/__init__.py`**

```python
from .database import Database
from .seed_taxonomy import seed_taxonomy

def init_db(path: str = "data/marketnews.db") -> Database:
    db = Database(path)
    db.init_schema()
    seed_taxonomy(db)
    return db

__all__ = ["Database", "init_db"]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/db/ -v`
Expected: 全部 passed

- [ ] **Step 5: 提交**

```bash
git add src/db/__init__.py tests/db/test_package_api.py
git commit -m "feat(db): add package API and init_db entry point"
```

---

## 自检（Self-Review）

- **Spec 覆盖**：本子计划覆盖设计文档第 5 节全部表（四层 + taxonomy/story_tag/entity/story_entity/watchlist + 产品层预留）。✔
- **占位符**：无 TBD/TODO；Task 6 给出全部方法签名 + 实现范式，非"同 Task N"含糊。✔
- **类型一致**：`RawItemRow`/`StoryRow`/`TaxonomyRow` 等字段与 schema 列名逐一对齐；`upsert` 返回 `(int, bool)` 全文一致。✔
- **依赖**：仅依赖 `sqlite-vec`、`pydantic`、`pytest`；`EMBEDDING_DIM=384` 与子计划3 的本地模型维度一致。✔
