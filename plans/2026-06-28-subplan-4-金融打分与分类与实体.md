# 子计划 4：金融打分 + 分类 + 实体抽取

> **致执行者：** 逐任务 TDD。前置：子计划 1、2、3 完成。

**目标：** 把 Horizon 的科技向打分改为「金融市场/估值重要性」打分；用一次 DeepSeek 调用对每个 story **同时**产出 0-10 分、五轴分类标签、实体（公司/标的）——分类与实体随打分顺带输出，几乎零额外 token；持久化 `score`/`story_tag`/`entity`/`story_entity`。打分只读 story 主条目的标题+摘要（省 token 的关卡）。

**架构：** 新增 `FinancialAnalyzer`，复用 Horizon 的 `src/ai/client.py::create_ai_client`（已支持 DeepSeek）。新增金融打分+分类的 system/user prompt。模型返回 JSON：`{score, rationale, tags:{market_type,industry_group,product_group,region,asset_class}, entities:[{type,name,ticker,role}]}`。`tags` 的值是 taxonomy 的 `code`，落库前用 `TaxonomyRepo.get_id(dimension, code)` 映射为 id；模型给出未知 code 时丢弃并记录。

**技术栈：** Horizon `ai/client.py`、子计划1 的 `ScoreRepo`/`StoryTagRepo`/`EntityRepo`/`StoryEntityRepo`/`TaxonomyRepo`/`StoryRepo`/`StoryMemberRepo`/`RawItemRepo`。

## Global Constraints
（同索引。）打分仅用标题+摘要；分类值必须落在受控词表内；JSON 输出按 schema 校验，解析失败只跳过该条不致命。

## 文件结构
- 创建 `src/ai/financial_prompts.py` — 金融打分+分类 prompt
- 创建 `src/analysis/__init__.py`
- 创建 `src/analysis/financial_analyzer.py` — `FinancialAnalyzer`
- 创建 `src/analysis/persist.py` — `AnalysisPersister`（把分析结果落库）
- 测试 `tests/analysis/`

---

## Task 1: 金融打分 + 分类 prompt

**Files:**
- Create: `src/ai/financial_prompts.py`
- Test: `tests/analysis/test_financial_prompts.py`

**Interfaces:**
- Produces:
  - `FINANCIAL_SCORE_SYSTEM: str`
  - `build_score_user(title: str, summary: str, taxonomy_codes: dict[str, list[str]]) -> str`（把受控词表 code 注入，要求模型只能从给定 code 里选）

- [ ] **Step 1: 写测试 `tests/analysis/test_financial_prompts.py`**

```python
from src.ai.financial_prompts import FINANCIAL_SCORE_SYSTEM, build_score_user

def test_system_prompt_is_market_oriented():
    s = FINANCIAL_SCORE_SYSTEM.lower()
    assert "market" in s or "估值" in FINANCIAL_SCORE_SYSTEM
    # 中性原则：打分阶段不产出投资建议
    assert "json" in s

def test_user_prompt_injects_allowed_codes():
    u = build_score_user("Acme buys Beta", "deal", {
        "market_type": ["primary","secondary"],
        "product_group": ["MA","ECM"]})
    assert "MA" in u and "secondary" in u
    assert "Acme buys Beta" in u
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/ai/financial_prompts.py`**

```python
"""金融市场重要性打分 + 五轴分类 + 实体抽取的提示词。"""
from __future__ import annotations
import json

FINANCIAL_SCORE_SYSTEM = """You are a financial markets news triage analyst. \
Score each item 0-10 by its importance to markets and asset valuation, classify it, \
and extract the entities (companies/instruments) involved.

Scoring guide (importance to markets & valuation):
- 9-10: Market-moving — major M&A, central bank policy shifts, large IPOs, systemic events
- 7-8:  High — notable deals/financings, earnings surprises, guidance changes, regulation
- 5-6:  Relevant — sector developments, mid-size raises, management changes
- 3-4:  Low — routine updates, minor filings
- 0-2:  Noise — promotional, off-topic, trivial

This is a NEUTRAL triage step: do NOT give buy/sell opinions or price targets. \
Only score, classify, and extract. Respond with VALID JSON only."""


def build_score_user(title: str, summary: str, taxonomy_codes: dict[str, list[str]]) -> str:
    codes = json.dumps(taxonomy_codes, ensure_ascii=False)
    return f"""News item:
TITLE: {title}
SUMMARY: {summary or "(none)"}

Allowed classification codes (choose only from these; a dimension may be null if unclear):
{codes}

Return JSON exactly:
{{
  "score": <float 0-10>,
  "rationale": "<one neutral sentence on why it matters to markets>",
  "tags": {{
    "market_type": "<code or null>",
    "industry_group": "<code or null>",
    "product_group": "<code or null>",
    "region": "<code or null>",
    "asset_class": "<code or null>"
  }},
  "entities": [{{"type": "company|ticker|sector|person", "name": "<name>", "ticker": "<or null>", "role": "primary|related"}}]
}}"""
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(ai): financial scoring+classification prompts"`

---

## Task 2: FinancialAnalyzer（一次调用产出分/类/实体）

**Files:**
- Create: `src/analysis/financial_analyzer.py`
- Test: `tests/analysis/test_financial_analyzer.py`

**Interfaces:**
- Consumes: Horizon `ai/client.py` 的 AI 客户端（有 `async complete(system, user) -> str` 等价方法；实现时对齐 `create_ai_client` 返回对象的实际方法名）、`TaxonomyRepo`
- Produces:
  - `@dataclass AnalysisResult(score:float, rationale:str, tag_codes:dict[str,str|None], entities:list[dict])`
  - `FinancialAnalyzer(ai_client, taxonomy_repo)`
  - `async analyze(title: str, summary: str) -> Optional[AnalysisResult]`（注入受控 code → 调模型 → 解析 JSON；失败返回 None）

- [ ] **Step 1: 写测试（注入假 AI 客户端，返回固定 JSON）`tests/analysis/test_financial_analyzer.py`**

```python
import asyncio, json
from src.db import init_db
from src.db.repositories.taxonomy_repo import TaxonomyRepo
from src.analysis.financial_analyzer import FinancialAnalyzer

class FakeAI:
    def __init__(self, payload): self.payload = payload
    async def complete(self, system, user): return self.payload

def test_analyze_parses_result(tmp_path):
    db = init_db(str(tmp_path/"t.db"))
    payload = json.dumps({"score":8.5,"rationale":"large cross-border M&A",
      "tags":{"market_type":"secondary","industry_group":"TMT","product_group":"MA",
              "region":"GreaterChina","asset_class":"Equity"},
      "entities":[{"type":"company","name":"Acme","ticker":None,"role":"primary"}]})
    fa = FinancialAnalyzer(FakeAI(payload), TaxonomyRepo(db))
    res = asyncio.run(fa.analyze("Acme buys Beta", "deal"))
    assert res.score == 8.5
    assert res.tag_codes["product_group"] == "MA"
    assert res.entities[0]["name"] == "Acme"

def test_analyze_handles_bad_json(tmp_path):
    db = init_db(str(tmp_path/"t.db"))
    fa = FinancialAnalyzer(FakeAI("not json"), TaxonomyRepo(db))
    assert asyncio.run(fa.analyze("x","y")) is None
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/analysis/financial_analyzer.py`**

```python
"""一次 LLM 调用产出 金融重要性分 + 五轴分类 + 实体。"""
from __future__ import annotations
import json
from dataclasses import dataclass
from typing import Optional
from ..ai.financial_prompts import FINANCIAL_SCORE_SYSTEM, build_score_user

_DIMENSIONS = ["market_type","industry_group","product_group","region","asset_class"]


@dataclass
class AnalysisResult:
    score: float
    rationale: str
    tag_codes: dict
    entities: list


class FinancialAnalyzer:
    def __init__(self, ai_client, taxonomy_repo):
        self.ai = ai_client
        self.tax = taxonomy_repo

    def _allowed_codes(self) -> dict[str, list[str]]:
        return {d: [t.code for t in self.tax.list_by_dimension(d)] for d in _DIMENSIONS}

    async def analyze(self, title: str, summary: str) -> Optional[AnalysisResult]:
        user = build_score_user(title, summary, self._allowed_codes())
        try:
            raw = await self.ai.complete(FINANCIAL_SCORE_SYSTEM, user)
            data = json.loads(_strip_fences(raw))
            return AnalysisResult(
                score=float(data["score"]),
                rationale=data.get("rationale", ""),
                tag_codes={d: (data.get("tags", {}) or {}).get(d) for d in _DIMENSIONS},
                entities=data.get("entities", []) or [],
            )
        except Exception:
            return None


def _strip_fences(text: str) -> str:
    t = text.strip()
    if t.startswith("```"):
        t = t.split("\n", 1)[1].rsplit("```", 1)[0]
    return t.strip()
```

> 注：`ai.complete(system, user)` 的实际签名以 Horizon `create_ai_client` 返回对象为准；若其方法名/参数不同，在此处适配（保持 `analyze` 对外签名不变）。

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(analysis): FinancialAnalyzer single-call score+tags+entities"`

---

## Task 3: AnalysisPersister（落库 score/story_tag/entity/story_entity）

**Files:**
- Create: `src/analysis/persist.py`
- Test: `tests/analysis/test_persist.py`

**Interfaces:**
- Consumes: `AnalysisResult`、`ScoreRepo`、`StoryTagRepo`、`EntityRepo`、`StoryEntityRepo`、`TaxonomyRepo`
- Produces:
  - `AnalysisPersister(score_repo, story_tag_repo, entity_repo, story_entity_repo, taxonomy_repo, model_name)`
  - `persist(story_id: int, result: AnalysisResult) -> None`：写 score；把每个非空 tag code 经 `taxonomy.get_id(dim, code)` 映射后写 story_tag（未知 code 跳过）；每个 entity `upsert` 后写 story_entity。

- [ ] **Step 1: 写测试 `tests/analysis/test_persist.py`**

```python
from datetime import datetime, timezone
from src.db import init_db
from src.db.repositories.story_repo import StoryRepo
from src.db.repositories.analysis_repo import ScoreRepo, StoryTagRepo
from src.db.repositories.entity_repo import EntityRepo, StoryEntityRepo
from src.db.repositories.taxonomy_repo import TaxonomyRepo
from src.db.rows import StoryRow
from src.analysis.financial_analyzer import AnalysisResult
from src.analysis.persist import AnalysisPersister

def test_persist_writes_all(tmp_path):
    db = init_db(str(tmp_path/"t.db"))
    now = datetime.now(timezone.utc).isoformat()
    sid = StoryRepo(db).create(StoryRow(canonical_title="Acme buys Beta",
        first_seen_at=now, last_seen_at=now))
    p = AnalysisPersister(ScoreRepo(db), StoryTagRepo(db), EntityRepo(db),
        StoryEntityRepo(db), TaxonomyRepo(db), model_name="deepseek-chat")
    res = AnalysisResult(8.0, "big deal",
        {"market_type":"secondary","industry_group":"TMT","product_group":"MA",
         "region":"GreaterChina","asset_class":"Equity"},
        [{"type":"company","name":"Acme","ticker":None,"role":"primary"}])
    p.persist(sid, res)
    assert ScoreRepo(db).latest_for_story(sid).score == 8.0
    assert len(StoryTagRepo(db).taxonomy_ids_for_story(sid)) == 5
    assert StoryEntityRepo(db).stories_for_entity(
        EntityRepo(db).upsert(__import__("src.db.rows", fromlist=["EntityRow"]).EntityRow(
            type="company", name="Acme", created_at=now))) == [sid]
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/analysis/persist.py`**

```python
"""把 AnalysisResult 落库。"""
from __future__ import annotations
from datetime import datetime, timezone
from ..db.rows import ScoreRow, EntityRow

_DIMENSIONS = ["market_type","industry_group","product_group","region","asset_class"]


class AnalysisPersister:
    def __init__(self, score_repo, story_tag_repo, entity_repo,
                 story_entity_repo, taxonomy_repo, model_name: str):
        self.scores = score_repo
        self.tags = story_tag_repo
        self.entities = entity_repo
        self.story_entities = story_entity_repo
        self.tax = taxonomy_repo
        self.model_name = model_name

    def persist(self, story_id: int, result) -> None:
        now = datetime.now(timezone.utc).isoformat()
        self.scores.add(ScoreRow(story_id=story_id, model=self.model_name,
            score=result.score, importance_rationale=result.rationale, scored_at=now))
        for dim in _DIMENSIONS:
            code = result.tag_codes.get(dim)
            if not code:
                continue
            tax_id = self.tax.get_id(dim, code)
            if tax_id is not None:
                self.tags.add(story_id, tax_id)
        for e in result.entities:
            if not e.get("name"):
                continue
            eid = self.entities.upsert(EntityRow(
                type=e.get("type", "company"), name=e["name"],
                ticker=e.get("ticker"), created_at=now))
            self.story_entities.add(story_id, eid, e.get("role", "related"))
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(analysis): persist score/tags/entities"`

---

## 自检
- 覆盖设计文档第 6 节第 3-4 步（打分 + 分类 + 实体）、第 7 节（打分只读标题+摘要、分类随打分顺带输出省 token）。✔
- 分类强制落在受控词表内（注入 code + 落库前 get_id 校验）。✔
- 打分阶段不产出建议（中性原则）。✔
- JSON 解析失败只跳过、不致命。✔
- 接口 `FinancialAnalyzer.analyze` 与计划索引一致。✔
