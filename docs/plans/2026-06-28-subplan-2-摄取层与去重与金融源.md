# 子计划 2：摄取层 + 持久化精确去重 + 金融数据源

> **致执行者：** 逐任务 TDD 执行。前置依赖：子计划 1（持久化层）已完成。读 `2026-06-28-phase1-计划索引.md`。

**目标：** 把 Horizon 各 scraper 抓回的 `ContentItem` 落库为 `raw_item`，按全局 `dedup_key` 幂等 upsert（已存在即跳过，**零 token**）；新增金融 RSS 源配置与两个新抓取器（SEC EDGAR/Form D、HKEX 披露易）。

**架构：** 新增 `IngestionService` 作为「Horizon scraper 输出 → 持久化层」的桥。`ContentItem→RawItemRow` 映射时计算 `canonical_url`（去 query/fragment 规范化）与 `content_hash`（标题+摘要哈希），`dedup_key = sha256(canonical_url || content_hash)`。新抓取器继承 `src/scrapers/base.py` 的 `BaseScraper`。

**技术栈：** Python 3.11+、`httpx`、`feedparser`、`hashlib`、子计划1 的 `RawItemRepo`。

## Global Constraints
（同计划索引全局约束。）异步并发抓取；新源失败只记录跳过、不中断整次运行；RSS 用 ETag/Last-Modified 条件请求。

## 文件结构
- 创建 `src/ingestion/__init__.py`
- 创建 `src/ingestion/mapper.py` — `to_raw_item(item: ContentItem, source_id: int|None) -> RawItemRow`，含 `canonical_url`、`content_hash`、`dedup_key`。
- 创建 `src/ingestion/service.py` — `IngestionService`。
- 创建 `src/scrapers/sec_edgar.py` — `SECEdgarScraper`（8-K + Form D）。
- 创建 `src/scrapers/hkex.py` — `HKEXScraper`（披露易公告）。
- 修改 `data/config.example.json` — 加入金融 RSS 源清单。
- 测试 `tests/ingestion/`。

---

## Task 1: ContentItem → RawItemRow 映射（含 dedup_key）

**Files:**
- Create: `src/ingestion/mapper.py`
- Test: `tests/ingestion/test_mapper.py`

**Interfaces:**
- Consumes: Horizon `src/models.py::ContentItem`、子计划1 `RawItemRow`
- Produces:
  - `canonicalize_url(url: str) -> str`（小写 scheme/host，去 fragment 与跟踪型 query 参数 utm_*，去尾斜杠）
  - `content_hash(title: str, summary: str|None) -> str`（sha256 hex）
  - `to_raw_item(item: ContentItem, source_id: Optional[int]) -> RawItemRow`，其中 `dedup_key = sha256(canonical_url + "|" + content_hash)`

- [ ] **Step 1: 写测试 `tests/ingestion/test_mapper.py`**

```python
from datetime import datetime, timezone
from src.models import ContentItem, SourceType
from src.ingestion.mapper import canonicalize_url, content_hash, to_raw_item

def _ci(url, title="Acme acquires Beta"):
    return ContentItem(
        id="rss:item:1", source_type=SourceType.RSS, title=title, url=url,
        content="summary text", published_at=datetime(2026,6,28,tzinfo=timezone.utc),
    )

def test_canonicalize_strips_utm_and_fragment():
    a = canonicalize_url("https://X.com/a?utm_source=x&id=5#frag")
    b = canonicalize_url("https://x.com/a?id=5")
    assert a == b

def test_same_event_same_dedup_key():
    r1 = to_raw_item(_ci("https://x.com/a?utm_source=tw"), source_id=1)
    r2 = to_raw_item(_ci("https://x.com/a"), source_id=2)
    assert r1.dedup_key == r2.dedup_key   # 跨源同一文章 → 同 key

def test_different_title_different_key():
    r1 = to_raw_item(_ci("https://x.com/a", "title one"), 1)
    r2 = to_raw_item(_ci("https://x.com/b", "title two"), 1)
    assert r1.dedup_key != r2.dedup_key
```

- [ ] **Step 2: 跑测试确认失败** — `pytest tests/ingestion/test_mapper.py -v`（ModuleNotFoundError）

- [ ] **Step 3: 实现 `src/ingestion/mapper.py`**

```python
"""ContentItem → RawItemRow 映射，计算规范 URL、内容哈希与全局 dedup_key。"""
from __future__ import annotations
import hashlib
from datetime import datetime, timezone
from typing import Optional
from urllib.parse import urlsplit, urlunsplit, parse_qsl, urlencode
from src.models import ContentItem
from src.db.rows import RawItemRow

_TRACKING_PREFIXES = ("utm_", "fbclid", "gclid", "mc_")


def canonicalize_url(url: str) -> str:
    parts = urlsplit(url.strip())
    scheme = parts.scheme.lower()
    netloc = parts.netloc.lower()
    query = [(k, v) for k, v in parse_qsl(parts.query)
             if not k.lower().startswith(_TRACKING_PREFIXES)]
    query.sort()
    path = parts.path.rstrip("/")
    return urlunsplit((scheme, netloc, path, urlencode(query), ""))


def content_hash(title: str, summary: Optional[str]) -> str:
    base = (title or "").strip() + "\n" + (summary or "").strip()
    return hashlib.sha256(base.encode("utf-8")).hexdigest()


def to_raw_item(item: ContentItem, source_id: Optional[int]) -> RawItemRow:
    canon = canonicalize_url(str(item.url))
    chash = content_hash(item.title, item.content)
    dedup = hashlib.sha256(f"{canon}|{chash}".encode("utf-8")).hexdigest()
    now = datetime.now(timezone.utc).isoformat()
    return RawItemRow(
        source_id=source_id,
        external_id=item.id,
        url=str(item.url),
        canonical_url=canon,
        title=item.title,
        summary=item.content,
        author=item.author,
        published_at=item.published_at.isoformat() if item.published_at else None,
        fetched_at=now,
        dedup_key=dedup,
        content_hash=chash,
        language=None,
        raw_payload=item.model_dump_json(),
    )
```

- [ ] **Step 4: 跑测试确认通过** — `pytest tests/ingestion/test_mapper.py -v`（3 passed）
- [ ] **Step 5: 提交** — `git commit -m "feat(ingestion): ContentItem→RawItemRow mapper with global dedup_key"`

---

## Task 2: IngestionService（幂等落库 + 统计）

**Files:**
- Create: `src/ingestion/service.py`
- Test: `tests/ingestion/test_service.py`

**Interfaces:**
- Consumes: `RawItemRepo`、`to_raw_item`、Horizon scrapers（返回 `List[ContentItem]`）
- Produces:
  - `@dataclass IngestStats(fetched:int, inserted:int, skipped:int)`
  - `IngestionService(repo: RawItemRepo)`
  - `ingest(items: list[ContentItem], source_id: Optional[int]=None) -> IngestStats`（对每条映射后 `repo.upsert`，统计新插/跳过）

- [ ] **Step 1: 写测试 `tests/ingestion/test_service.py`**

```python
from datetime import datetime, timezone
from src.models import ContentItem, SourceType
from src.db import init_db
from src.db.repositories.raw_item_repo import RawItemRepo
from src.ingestion.service import IngestionService

def _ci(i):
    return ContentItem(id=f"rss:item:{i}", source_type=SourceType.RSS,
        title=f"news {i}", url=f"https://x.com/{i}", content="c",
        published_at=datetime(2026,6,28,tzinfo=timezone.utc))

def test_ingest_dedups_across_runs(tmp_path):
    db = init_db(str(tmp_path / "t.db"))
    svc = IngestionService(RawItemRepo(db))
    s1 = svc.ingest([_ci(1), _ci(2)])
    s2 = svc.ingest([_ci(1), _ci(2), _ci(3)])   # 1,2 重复
    assert s1.inserted == 2 and s1.skipped == 0
    assert s2.inserted == 1 and s2.skipped == 2
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/ingestion/service.py`**

```python
"""摄取服务：映射并幂等落库，挡住重复（在任何模型调用之前）。"""
from __future__ import annotations
from dataclasses import dataclass
from typing import Optional
from src.models import ContentItem
from src.db.repositories.raw_item_repo import RawItemRepo
from .mapper import to_raw_item


@dataclass
class IngestStats:
    fetched: int = 0
    inserted: int = 0
    skipped: int = 0


class IngestionService:
    def __init__(self, repo: RawItemRepo):
        self.repo = repo

    def ingest(self, items: list[ContentItem], source_id: Optional[int] = None) -> IngestStats:
        stats = IngestStats(fetched=len(items))
        for item in items:
            _id, is_new = self.repo.upsert(to_raw_item(item, source_id))
            if is_new:
                stats.inserted += 1
            else:
                stats.skipped += 1
        return stats
```

- [ ] **Step 4: 跑测试确认通过**（1 passed）
- [ ] **Step 5: 提交** — `git commit -m "feat(ingestion): IngestionService with cross-run dedup stats"`

---

## Task 3: SECEdgarScraper（8-K + Form D）

**Files:**
- Create: `src/scrapers/sec_edgar.py`
- Test: `tests/scrapers/test_sec_edgar.py`

**Interfaces:**
- Consumes: `BaseScraper`、`ContentItem`、`SourceType`
- Produces: `SECEdgarScraper(config: dict, http_client)`，`async fetch(since) -> List[ContentItem]`。用 EDGAR full-text/atom：`https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&type=8-K&output=atom` 及 Form D（`type=D`）。需带 `User-Agent`（SEC 要求）。把每条申报映射为 `ContentItem(source_type=RSS or 新增 EDGAR 子类型, url=申报链接, title=公司+表单类型, content=摘要)`。

- [ ] **Step 1: 写测试（用本地 atom 样例字符串，离线解析）`tests/scrapers/test_sec_edgar.py`**

```python
import asyncio, httpx
from src.scrapers.sec_edgar import parse_edgar_atom

SAMPLE = """<?xml version='1.0'?>
<feed xmlns='http://www.w3.org/2005/Atom'>
 <entry><title>8-K - ACME CORP (0000123)</title>
  <link href='https://www.sec.gov/x/8k.htm'/>
  <updated>2026-06-28T10:00:00-04:00</updated>
  <summary>Item 1.01 Entry into a Material Definitive Agreement</summary>
 </entry></feed>"""

def test_parse_edgar_atom():
    items = parse_edgar_atom(SAMPLE, form_type="8-K")
    assert len(items) == 1
    assert items[0].title.startswith("8-K")
    assert str(items[0].url).endswith("8k.htm")
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/scrapers/sec_edgar.py`**（解析函数纯净可测；fetch 调它）

```python
"""SEC EDGAR 抓取器：8-K（重大事件/并购）与 Form D（私募融资）。"""
from __future__ import annotations
from datetime import datetime, timezone
from typing import List
import feedparser
from .base import BaseScraper
from ..models import ContentItem, SourceType

_FEEDS = {
    "8-K": "https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&type=8-K&output=atom&count=100",
    "D":   "https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&type=D&output=atom&count=100",
}


def parse_edgar_atom(xml: str, form_type: str) -> List[ContentItem]:
    feed = feedparser.parse(xml)
    out: List[ContentItem] = []
    for e in feed.entries:
        link = e.get("link") or (e.links[0].href if e.get("links") else "")
        pub = e.get("updated") or e.get("published")
        try:
            published = datetime(*e.updated_parsed[:6], tzinfo=timezone.utc)
        except Exception:
            published = datetime.now(timezone.utc)
        out.append(ContentItem(
            id=f"edgar:{form_type}:{link}", source_type=SourceType.RSS,
            title=e.get("title", f"{form_type} filing"), url=link,
            content=e.get("summary", ""), published_at=published,
        ))
    return out


class SECEdgarScraper(BaseScraper):
    async def fetch(self, since: datetime) -> List[ContentItem]:
        items: List[ContentItem] = []
        headers = {"User-Agent": self.config.get(
            "user_agent", "MarketNews research contact@example.com")}
        for form_type in self.config.get("forms", ["8-K", "D"]):
            try:
                resp = await self.client.get(_FEEDS[form_type], headers=headers, timeout=30)
                items.extend([i for i in parse_edgar_atom(resp.text, form_type)
                              if i.published_at >= since])
            except Exception:
                continue   # 单源失败跳过，不中断
        return items
```

- [ ] **Step 4: 跑测试确认通过** — `pytest tests/scrapers/test_sec_edgar.py -v`
- [ ] **Step 5: 提交** — `git commit -m "feat(scrapers): add SEC EDGAR scraper (8-K + Form D)"`

---

## Task 4: HKEXScraper（披露易公告）

**Files:**
- Create: `src/scrapers/hkex.py`
- Test: `tests/scrapers/test_hkex.py`

**Interfaces:**
- Produces: `HKEXScraper(config, http_client)`，`async fetch(since)`。HKEX 披露易公告 API（`https://www1.hkexnews.hk/...` 的 JSON 列表接口；具体端点在 `fetch_config` 配置）。提供纯函数 `parse_hkex_json(data: dict) -> List[ContentItem]` 供离线测试。

- [ ] **Step 1: 写测试（本地 JSON 样例）`tests/scrapers/test_hkex.py`**

```python
from src.scrapers.hkex import parse_hkex_json

SAMPLE = {"newslist": [
  {"NEWS_ID":"1","TITLE":"DISCLOSEABLE TRANSACTION - Acquisition",
   "STOCK_CODE":"0700","DATE_TIME":"2026-06-28 08:00",
   "FILE_LINK":"https://www1.hkexnews.hk/x.pdf"}]}

def test_parse_hkex_json():
    items = parse_hkex_json(SAMPLE)
    assert len(items) == 1
    assert "Acquisition" in items[0].title
    assert str(items[0].url).endswith(".pdf")
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `src/scrapers/hkex.py`**

```python
"""HKEX 披露易公告抓取器。"""
from __future__ import annotations
from datetime import datetime, timezone
from typing import List
from .base import BaseScraper
from ..models import ContentItem, SourceType


def parse_hkex_json(data: dict) -> List[ContentItem]:
    out: List[ContentItem] = []
    for n in data.get("newslist", []):
        try:
            published = datetime.strptime(n["DATE_TIME"], "%Y-%m-%d %H:%M").replace(
                tzinfo=timezone.utc)
        except Exception:
            published = datetime.now(timezone.utc)
        out.append(ContentItem(
            id=f"hkex:{n.get('NEWS_ID')}", source_type=SourceType.RSS,
            title=f"[{n.get('STOCK_CODE','')}] {n.get('TITLE','')}".strip(),
            url=n.get("FILE_LINK"), content=n.get("TITLE", ""),
            published_at=published,
        ))
    return out


class HKEXScraper(BaseScraper):
    async def fetch(self, since: datetime) -> List[ContentItem]:
        url = self.config.get("endpoint")
        if not url:
            return []
        try:
            resp = await self.client.get(url, timeout=30)
            return [i for i in parse_hkex_json(resp.json()) if i.published_at >= since]
        except Exception:
            return []
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(scrapers): add HKEX disclosure scraper"`

---

## Task 5: 金融 RSS 源配置

**Files:**
- Modify: `data/config.example.json`（在 `sources.rss` 加入金融源）
- Test: `tests/ingestion/test_config_sources.py`

- [ ] **Step 1: 写测试**：读取 `data/config.example.json`，断言 `sources.rss` 含 Reuters/FT/CNBC/SCMP/Nikkei 等金融源域名，且每条有 `name`/`url`/`enabled`。
- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 在 config 加入金融源**（示例条目）

```json
{
  "sources": {
    "rss": [
      {"name": "Reuters Business", "url": "https://www.reutersagency.com/feed/?best-topics=business-finance", "enabled": true},
      {"name": "FT Companies", "url": "https://www.ft.com/companies?format=rss", "enabled": true},
      {"name": "CNBC Finance", "url": "https://www.cnbc.com/id/10000664/device/rss/rss.html", "enabled": true},
      {"name": "MarketWatch Top", "url": "https://feeds.marketwatch.com/marketwatch/topstories/", "enabled": true},
      {"name": "SCMP Business", "url": "https://www.scmp.com/rss/92/feed", "enabled": true},
      {"name": "Nikkei Asia Markets", "url": "https://asia.nikkei.com/rss/feed/nar", "enabled": true},
      {"name": "PE Hub", "url": "https://www.pehub.com/feed/", "enabled": true}
    ]
  }
}
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(config): add global + APAC financial RSS sources"`

---

## 自检
- 覆盖设计文档第 4 节数据源（全球 + 亚太 + SEC EDGAR/Form D + HKEX）、第 6 节第 1-2 步（抓取 + 精确去重）。✔
- 精确去重在 `IngestionService.ingest` 内，先于一切模型调用，重复条目零处理。✔
- 新源失败跳过、不中断；纯解析函数可离线测试，不依赖网络。✔
- 接口 `IngestionService.ingest`、`SECEdgarScraper`、`HKEXScraper` 与计划索引一致。✔
